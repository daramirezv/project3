import { Meteor } from 'meteor/meteor';
import '../imports/api/viajes.js';
import '../imports/api/mensajes.js';
import '../imports/api/companies.js';

Meteor.startup(() => {
  // code to run on server at startup
});
